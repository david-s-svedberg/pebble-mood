#pragma once

// A metric checklist for choosing the home-screen graph favourites (max
// MAX_FAVORITES). Select toggles a metric; adds are ignored once full.
void setup_favorites_window();
void tear_down_favorites_window();
